@page "/{TeamCode}/players/{PlayerId:int}"
@layout TeamLayout
@rendermode InteractiveServer
@attribute [Authorize]
@inject TeamAccessService TeamAccess
@inject PlayerService PlayerService
@inject StatsService StatsService
@inject MatchService MatchService
@inject CurrentUserService CurrentUser
@inject IJSRuntime JS

<PageTitle>@(_player?.Name ?? "Player") - Foosball Manager</PageTitle>

@if (_loading)
{
    <LoadingSpinner />
}
else if (_player != null && _stats != null)
{
    <div class="container py-4">
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/@TeamCode">Dashboard</a></li>
                <li class="breadcrumb-item"><a href="/@TeamCode/players">Players</a></li>
                <li class="breadcrumb-item active">@_player.Name</li>
            </ol>
        </nav>

        <div class="row">
            <div class="col-lg-4 mb-4">
                <div class="card @(_isMyPlayer ? "border-success" : "")">
                    <div class="card-body text-center">
                        <Avatar AvatarId="@_player.AvatarId" Size="xl" />
                        <h3 class="mt-3 mb-1">
                            @_player.Name
                            @if (_isMyPlayer)
                            {
                                <span class="badge badge-you ms-1">you</span>
                            }
                        </h3>
                        @if (!string.IsNullOrEmpty(_player.User?.UserName))
                        {
                            <div class="text-muted small mb-2">@($"@{_player.User.UserName.ToLowerInvariant()}")</div>
                        }
                        <h4 class="text-success">@_stats.CurrentElo ELO</h4>
                        @if (!_player.IsActive)
                        {
                            <span class="badge bg-secondary">Inactive</span>
                        }
                        @if (_canEdit)
                        {
                            <div class="mt-3">
                                <button class="btn btn-sm btn-outline-secondary" @onclick="ShowEditModal">
                                    <i class="bi bi-pencil me-1"></i> Edit name &amp; avatar
                                </button>
                            </div>
                        }

                        <hr />

                        <div class="row text-center">
                            <div class="col-4">
                                <h5 class="mb-0">@_stats.TotalMatches</h5>
                                <small class="text-muted">Matches</small>
                            </div>
                            <div class="col-4">
                                <h5 class="mb-0 text-success">@_stats.Wins</h5>
                                <small class="text-muted">Wins</small>
                            </div>
                            <div class="col-4">
                                <h5 class="mb-0 text-danger">@_stats.Losses</h5>
                                <small class="text-muted">Losses</small>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card mt-3">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="bi bi-graph-up me-2"></i>Statistics</h6>
                    </div>
                    <ul class="list-group list-group-flush">
                        @StatRow("Win Rate", $"{_stats.WinRate:F1}%")
                        @StatRow("Highest ELO", _stats.HighestElo.ToString(), valueClass: "text-success")
                        @StatRow("Lowest ELO", _stats.LowestElo.ToString(), valueClass: "text-danger")
                        <li class="list-group-item d-flex justify-content-between">
                            <span>Current Streak</span>
                            <strong class="@(_stats.CurrentStreak > 0 ? "text-success" : _stats.CurrentStreak < 0 ? "text-danger" : "")">
                                @(_stats.CurrentStreak > 0 ? $"+{_stats.CurrentStreak}" : _stats.CurrentStreak.ToString())
                            </strong>
                        </li>
                        @StatRow("Longest Win Streak", _stats.LongestWinStreak.ToString(), valueClass: "text-success")
                        @StatRow("Longest Loss Streak", _stats.LongestLossStreak.ToString(), valueClass: "text-danger")
                        @if (_stats.RecentForm.Count > 0)
                        {
                            <li class="list-group-item">
                                <div class="d-flex justify-content-between mb-1">
                                    <span title="Win rate across the last @_stats.RecentForm.Count matches">Recent Form</span>
                                    <strong class="@(_stats.RecentFormWinRate >= 50 ? "text-success" : "text-danger")">@_stats.RecentFormWinRate.ToString("F0")%</strong>
                                </div>
                                <div class="d-flex gap-1 flex-wrap">
                                    @foreach (var won in _stats.RecentForm)
                                    {
                                        <span class="badge @(won ? "bg-success" : "bg-danger")" style="min-width:1.5rem">@(won ? "W" : "L")</span>
                                    }
                                </div>
                            </li>
                        }
                        @StatRow("Avg ELO / Win", $"{(_stats.AvgEloChangeOnWin > 0 ? "+" : "")}{_stats.AvgEloChangeOnWin:F1}",
                                 tooltip: "Average ELO gained per win", valueClass: "text-success")
                        @StatRow("Avg ELO / Loss", _stats.AvgEloChangeOnLoss.ToString("F1"),
                                 tooltip: "Average ELO lost per loss", valueClass: "text-danger")
                        @StatRow("Avg Opponent ELO", _stats.AvgOpponentElo.ToString(),
                                 tooltip: "Average ELO of opponents faced (at time of match)")
                        @StatRow("Avg Teammate ELO", _stats.AvgTeammateElo.ToString(),
                                 tooltip: "Average ELO of teammates (at time of match)")
                        <li class="list-group-item d-flex justify-content-between" title="How evenly games are distributed across the current active roster (100% = perfectly even across all @_stats.ActiveRosterPartners teammates). Needs at least @Constants.TimeThresholds.MinGamesForVarietyBadge games with active teammates.">
                            <span>Teammate Variety</span>
                            <strong>
                                @if (_stats.HasEnoughGamesForVariety && _stats.ActiveRosterPartners >= 2)
                                {
                                    @($"{Math.Round(_stats.TeammateVariety * 100)}%")
                                    <small class="text-muted ms-1">(@_stats.UniqueTeammates/@_stats.ActiveRosterPartners)</small>
                                }
                                else
                                {
                                    <span class="text-muted fst-italic">n/a</span>
                                }
                            </strong>
                        </li>
                        @StatRow("Preferred Position", _stats.PreferredPosition)
                        @StatRow("Better Position", _stats.BetterPosition,
                                 tooltip: $"Position with the higher win rate (min {Constants.TimeThresholds.MinGamesForPartnerStats} games per position)")
                        @StatRow("Under Table Losses", _stats.UnderTableCount.ToString(), valueClass: "text-danger")
                        @StatRow("Under Table Wins", _stats.TableSenderCount.ToString(), valueClass: "text-success")
                        <li class="list-group-item d-flex justify-content-between" title="Wins where a stronger partner (20%+ higher ELO) and both weaker opponents made the win expected — i.e. this player was carried">
                            <span>Carried</span>
                            <strong>
                                @_stats.CarriedCount
                                @if (_stats.Wins > 0 && _stats.CarriedCount > 0)
                                {
                                    <small class="text-muted ms-1">(@(((double)_stats.CarriedCount / _stats.Wins * 100).ToString("F0"))% of wins)</small>
                                }
                            </strong>
                        </li>
                        <li class="list-group-item d-flex justify-content-between" title="Wins where this player was the stronger partner (20%+ higher ELO) and both opponents were weaker — i.e. this player carried">
                            <span>Carries</span>
                            <strong>
                                @_stats.CarryCount
                                @if (_stats.Wins > 0 && _stats.CarryCount > 0)
                                {
                                    <small class="text-muted ms-1">(@(((double)_stats.CarryCount / _stats.Wins * 100).ToString("F0"))% of wins)</small>
                                }
                            </strong>
                        </li>
                    </ul>
                </div>

                <div class="card mt-3">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="bi bi-person-badge me-2"></i>Position Stats</h6>
                    </div>
                    <ul class="list-group list-group-flush">
                        @PositionRow("Goalkeeper", "bi bi-shield", _stats.GamesAsGk, _stats.GoalsScoredAsGk, _stats.GoalsConcededAsGk, _stats.WinRateAsGk, _stats.WinsAsGk)
                        @PositionRow("Attacker", "bi bi-bullseye", _stats.GamesAsAtk, _stats.GoalsScoredAsAtk, _stats.GoalsConcededAsAtk, _stats.WinRateAsAtk, _stats.WinsAsAtk)
                    </ul>
                </div>

                <div class="row mt-3">
                    <div class="col-6 mb-3">
                        <PartnerStatCard Title="Best Partner" Icon="bi bi-hand-thumbs-up" Variant="success"
                                         PlayerName="@_stats.BestPartner" WinRate="_stats.BestPartnerWinRate" Games="_stats.BestPartnerGames" />
                    </div>
                    <div class="col-6 mb-3">
                        <PartnerStatCard Title="Worst Partner" Icon="bi bi-hand-thumbs-down" Variant="danger"
                                         PlayerName="@_stats.WorstPartner" WinRate="_stats.WorstPartnerWinRate" Games="_stats.WorstPartnerGames"
                                         HighlightWin="false" />
                    </div>
                    <div class="col-6 mb-3">
                        <PartnerStatCard Title="Easiest Enemy" Icon="bi bi-emoji-smile" Variant="info"
                                         PlayerName="@_stats.EasiestEnemy" WinRate="_stats.EasiestEnemyWinRate" Games="_stats.EasiestEnemyGames" />
                    </div>
                    <div class="col-6 mb-3">
                        <PartnerStatCard Title="Hardest Enemy" Icon="bi bi-emoji-frown" Variant="warning"
                                         PlayerName="@_stats.HardestEnemy" WinRate="_stats.HardestEnemyWinRate" Games="_stats.HardestEnemyGames"
                                         HighlightWin="false" />
                    </div>
                </div>
            </div>

            <div class="col-lg-8">
                @if (_stats.EloHistory.Count > 1)
                {
                    <div class="card mb-4">
                        <div class="card-header">
                            <h6 class="mb-0"><i class="bi bi-graph-up-arrow me-2"></i>ELO History</h6>
                        </div>
                        <div class="card-body">
                            <canvas id="eloChart" height="200"></canvas>
                        </div>
                    </div>
                }

                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h6 class="mb-0"><i class="bi bi-clock-history me-2"></i>Recent Matches</h6>
                        @if (_totalMatchCount > Constants.Pagination.PlayerDetailRecentMatches)
                        {
                            <a href="/@TeamCode/matches?player=@PlayerId" class="btn btn-sm btn-outline-success">View All (@_totalMatchCount)</a>
                        }
                    </div>
                    <div class="card-body">
                        @if (_recentMatches.Count > 0 && _lastLoadedPlayerId == PlayerId)
                        {
                            @foreach (var match in _recentMatches)
                            {
                                var playerMatch = match.MatchPlayers.FirstOrDefault(mp => mp.PlayerId == PlayerId);
                                if (playerMatch != null)
                                {
                                    var playerTeamWon = (playerMatch.TeamNumber == 1 && match.Team1Score > match.Team2Score) ||
                                                       (playerMatch.TeamNumber == 2 && match.Team2Score > match.Team1Score);
                                    <div class="match-history-item mb-2 p-2 rounded @(playerTeamWon ? "bg-success bg-opacity-10" : "bg-danger bg-opacity-10")">
                                        <MatchCard Match="@match" TeamCode="@TeamCode" Compact="true" />
                                        <div class="d-flex justify-content-between align-items-center small px-2">
                                            <span class="text-muted">
                                                <i class="bi bi-calendar3 me-1"></i>
                                                <span class="match-date" data-utc="@match.PlayedAt.ToString("o")">@match.PlayedAt.ToString("MMM dd, yyyy")</span>
                                            </span>
                                            <span class="@(playerTeamWon ? "text-success" : "text-danger") fw-bold">
                                                @(playerMatch.EloChange > 0 ? $"+{playerMatch.EloChange}" : playerMatch.EloChange.ToString()) ELO
                                            </span>
                                        </div>
                                    </div>
                                }
                            }
                        }
                        else
                        {
                            <EmptyState Icon="bi bi-calendar-x" IconSize="md" Padding="py-3" Message="No matches played yet." />
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <Modal Show="@(_showEditModal && _player != null)" Title="Edit player" OnClose="HideEditModal">
        <Body>
            <AlertMessage Message="@_editError" Variant="danger" />
            <div class="mb-3">
                <label for="editName" class="form-label">Name</label>
                <input id="editName" class="form-control" @bind="_editName" maxlength="50" />
            </div>
            <div class="mb-3">
                <label class="form-label">Avatar</label>
                <AvatarPicker @bind-Value="_editAvatar" />
            </div>
        </Body>
        <Footer>
            <button type="button" class="btn btn-secondary" @onclick="HideEditModal">Cancel</button>
            <SpinnerButton Busy="_savingEdit" OnClick="SaveEdit">Save</SpinnerButton>
        </Footer>
    </Modal>
}
else
{
    <div class="container py-4">
        <AlertMessage Message="Player not found." Variant="warning" Icon="bi bi-exclamation-triangle" />
        <a href="/@TeamCode/players" class="btn btn-primary">Back to Players</a>
    </div>
}

@code {
    [Parameter] public string TeamCode { get; set; } = string.Empty;
    [Parameter] public int PlayerId { get; set; }

    private Team? _team;
    private Player? _player;
    private PlayerStats? _stats;
    private List<Match> _recentMatches = [];
    private int _totalMatchCount;
    private bool _loading = true;
    private int _lastLoadedPlayerId;
    private bool _chartRendered;
    private bool _isMyPlayer;
    private bool _canEdit;
    private bool _showEditModal;
    private string _editName = string.Empty;
    private int _editAvatar = 1;
    private string? _editError;
    private bool _savingEdit;

    [CascadingParameter(Name = "MyPlayerId")] public int? MyPlayerId { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender || (_lastLoadedPlayerId != PlayerId && !_loading))
        {
            await LoadPlayerDataAsync();
        }
        else if (_stats?.EloHistory.Count > 1 && !_chartRendered)
        {
            await RenderChart();
            _chartRendered = true;
        }
    }

    private async Task LoadPlayerDataAsync()
    {
        _loading = true;
        _chartRendered = false;
        StateHasChanged();

        _team = await TeamAccess.GetCurrentTeamAsync();
        if (_team != null)
        {
            _player = await PlayerService.GetByIdWithTeamAsync(PlayerId);
            if (_player != null && _player.TeamId == _team.Id)
            {
                _stats = await StatsService.GetPlayerStatsAsync(PlayerId);
                _recentMatches = await MatchService.GetByPlayerAsync(PlayerId, Constants.Pagination.PlayerDetailRecentMatches);
                _totalMatchCount = await MatchService.GetCountByPlayerAsync(PlayerId);

                var userId = await CurrentUser.GetUserIdAsync();
                var isAdmin = await TeamAccess.IsAdminAsync(_team);
                _isMyPlayer = userId != null && _player.UserId == userId;
                _canEdit = isAdmin || _isMyPlayer;
            }
            else
            {
                _player = null;
                _stats = null;
                _recentMatches = [];
                _totalMatchCount = 0;
                _isMyPlayer = false;
                _canEdit = false;
            }
        }
        _lastLoadedPlayerId = PlayerId;
        _loading = false;
        StateHasChanged();

        if (_stats?.EloHistory.Count > 1)
        {
            await RenderChart();
            _chartRendered = true;
        }
    }

    private void ShowEditModal()
    {
        if (_player == null) return;
        _editName = _player.Name;
        _editAvatar = _player.AvatarId;
        _editError = null;
        _showEditModal = true;
    }

    private void HideEditModal()
    {
        _showEditModal = false;
        _editError = null;
    }

    private async Task SaveEdit()
    {
        if (_team == null || _player == null) return;
        var name = _editName?.Trim() ?? string.Empty;
        if (name.Length < 1 || name.Length > 50)
        {
            _editError = "Name must be 1-50 characters.";
            return;
        }

        _savingEdit = true;
        _editError = null;
        try
        {
            if (await PlayerService.IsNameTakenAsync(_team.Id, name, _player.Id))
            {
                _editError = "A player with that name already exists in this team.";
                return;
            }
            var actorId = await CurrentUser.GetUserIdAsync();
            if (actorId == null)
            {
                _editError = "You must be logged in.";
                return;
            }
            var ok = await PlayerService.UpdateAsync(_player.Id, _team.Id, actorId.Value, name, _editAvatar);
            if (!ok)
            {
                _editError = "Could not update. You may not have permission.";
                return;
            }
            _showEditModal = false;
            await LoadPlayerDataAsync();
        }
        finally
        {
            _savingEdit = false;
            StateHasChanged();
        }
    }

    private async Task RenderChart()
    {
        if (_stats == null) return;

        var labels = _stats.EloHistory.Select(h => h.Date.ToString("MMM dd")).ToArray();
        var data = _stats.EloHistory.Select(h => h.Elo).ToArray();

        await JS.InvokeVoidAsync("renderEloChart", "eloChart", labels, data);
    }

    private static RenderFragment StatRow(string label, string value, string? tooltip = null, string? valueClass = null) => __builder =>
    {
        <li class="list-group-item d-flex justify-content-between" title="@tooltip">
            <span>@label</span>
            <strong class="@valueClass">@value</strong>
        </li>;
    };

    private static RenderFragment PositionRow(string label, string icon, int games, int scored, int conceded, double winRate, int wins) => __builder =>
    {
        <li class="list-group-item">
            <div class="d-flex justify-content-between mb-1">
                <span><i class="@icon me-1"></i>As @label</span>
                <small class="text-muted">@games games</small>
            </div>
            @if (games > 0)
            {
                <div class="small">
                    <span class="text-success">Scored: @scored (@(((double)scored / games).ToString("F1"))/game)</span>
                    <span class="mx-2">|</span>
                    <span class="text-danger">Conceded: @conceded (@(((double)conceded / games).ToString("F1"))/game)</span>
                </div>
                <div class="small mt-1">
                    <span class="@(winRate >= 50 ? "text-success" : "text-danger")">Win Rate: @winRate.ToString("F1")%</span>
                    <span class="text-muted">(@wins W / @(games - wins) L)</span>
                </div>
            }
        </li>;
    };
}
